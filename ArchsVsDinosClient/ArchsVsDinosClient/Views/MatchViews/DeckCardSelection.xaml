<UserControl x:Class="ArchsVsDinosClient.Views.MatchViews.DeckCardSelection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Width="80" Height="120"
             Cursor="Hand">

    <Border x:Name="BorderGlow" BorderThickness="0" CornerRadius="8" BorderBrush="#FFD700" SnapsToDevicePixels="True" RenderTransformOrigin="0.5,1">
        <Border.Effect>
            <DropShadowEffect x:Name="GlowEffect" Color="#FFD700" BlurRadius="0" ShadowDepth="0" Opacity="0"/>
        </Border.Effect>
        
        <Border.RenderTransform>
            <ScaleTransform x:Name="CardTransform" ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>

        <Grid>
            <Image x:Name="CardImage" Source="{Binding CardRoute}" Stretch="Fill" RenderOptions.BitmapScalingMode="HighQuality"/>
        </Grid>
    </Border>
</UserControl>