<Window x:Class="ArchsVsDinosClient.Views.MatchViews.MatchSeeDeck.MatchSeeDeckHorizontal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArchsVsDinosClient.Views.MatchViews.MatchSeeDeck" xmlns:controls="clr-namespace:ArchsVsDinosClient.Views.MatchViews"
        mc:Ignorable="d"
        Title="WatchDeckHorizontal" Height="540" Width="1320" ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen">

    <Window.Background>
        <ImageBrush ImageSource="/Resources/Images/seeDeckHorizontalBackground.png"/>
    </Window.Background>

    <Grid Margin="10,10,10,45">

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="16*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="1">
                <Button Click="Click_BtnClose">
                    <Button.Background>
                        <ImageBrush ImageSource="/Resources/Images/close.png"/>
                    </Button.Background>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="5" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Grid>
                <controls:PlayerCellHorizontal x:Name="TopPlayerCards"/>
            </Grid>
        </Grid>
        
    </Grid>
    
</Window>
