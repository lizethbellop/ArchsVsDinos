<Window x:Class="ArchsVsDinosClient.Views.MatchViews.MatchDiscardPile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArchsVsDinosClient.Views.MatchViews"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:ArchsVsDinosClient.Properties.Langs"
        Title="MatchDiscardPile" Height="500" Width="1400"
        WindowStartupLocation="CenterScreen" WindowStyle="None">

    <Window.Background>
        <ImageBrush ImageSource="/Resources/Images/trashBack.png"/>
    </Window.Background>

    <Window.Resources>
        <DropShadowEffect x:Key="DropShadowEffect" Color="Black" BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
    </Window.Resources>

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height=".5*"/>
        </Grid.RowDefinitions>

        <!--Header-->
        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="13*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Label Content="{x:Static language:Lang.MatchDiscard_DiscardPile}" FontFamily="{StaticResource Volcanic Dungeon}" 
                       FontSize="25" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

            <Grid Grid.Column="1">
                <Button Grid.Column="2" Click="Click_BtnClose" Width="70" Height="70">
                    <Button.Background>
                        <ImageBrush ImageSource="/Resources/Images/close.png"/>
                    </Button.Background>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"  CornerRadius="5" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>

        <!--Cards-->
        <Grid Grid.Row="1" Margin="10">
            <Border Background="#1a000000" 
                    CornerRadius="15" 
                    BorderBrush="#40FFFFFF" 
                    BorderThickness="2" 
                    Padding="10">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" 
                             VerticalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="IC_DiscardedCards">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" MaxHeight="380"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Background="Transparent" BorderThickness="0" Padding="0" Margin="8" 
                                       Cursor="Hand" Click="Click_BtnSelectCard" Tag="{Binding IdCard}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="cardBorder" Background="#2a2a2a" BorderBrush="#555" BorderThickness="3" CornerRadius="12" 
                                                   Width="150" Height="200" Effect="{StaticResource DropShadowEffect}">

                                                <Border ClipToBounds="True" Margin="5">
                                                    <Image Source="{Binding CardRoute}" Stretch="Uniform"/>
                                                </Border>
                                            </Border>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="cardBorder" Property="BorderBrush" Value="#FFD700"/>
                                                    <Setter TargetName="cardBorder" Property="BorderThickness" Value="5"/>
                                                    <Setter TargetName="cardBorder" Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect Color="#FFD700" 
                                                                            BlurRadius="30" 
                                                                            ShadowDepth="0"
                                                                            Opacity="1"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>

                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="cardBorder" Property="BorderBrush" Value="#FF8C00"/>
                                                    <Setter TargetName="cardBorder" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <ScaleTransform ScaleX="0.92" ScaleY="0.92"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter TargetName="cardBorder" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>

        <!--Footer-->
        <Grid Grid.Row="2">
            <Label Content="{x:Static language:Lang.MatchDiscard_TakeACardWarning}" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="16" Foreground="#FFFFFDFD" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        
    </Grid>
    
</Window>
