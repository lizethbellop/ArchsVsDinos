<Window x:Class="ArchsVsDinosClient.Views.LoginRecoverPassword.EnterCode"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArchsVsDinosClient.Views.LoginRecoverPassword"
        xmlns:language="clr-namespace:ArchsVsDinosClient.Properties.Langs"
        mc:Ignorable="d"
        Title="Enter Code" Height="300" Width="450"        
        WindowStartupLocation="CenterScreen" WindowStyle="None">

    <Window.Background>

        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FF303168"/>
            <GradientStop Color="#FFFFEBDC" Offset="1"/>
            <GradientStop Color="#FF8346A4" Offset="0.42"/>
            <GradientStop Color="#FEDCB77B" Offset="0.715"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid Margin="15,15,15,15">
        <Grid.RowDefinitions>
            <!--Title  -->
            <RowDefinition Height="Auto"/>
            <!-- Indication -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Label Content="{x:Static language:Lang.ChangeP_Title}" FontFamily="{StaticResource Volcanic Dungeon}" FontSize="14" FontWeight="Bold" Foreground="White"/>
        </StackPanel>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <Label Content="{x:Static language:Lang.ChangeP_EnterCode}" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="15" HorizontalAlignment="Center" Foreground="White"/>
            <Border CornerRadius="15" Background="White" BorderBrush="Gray" BorderThickness="1"  Width="268" Height="29" VerticalAlignment="Top">
                <TextBox Name = "TxtB_NewPassword" MaxLength="5" TextWrapping="NoWrap" FontFamily="Trebuchet MS" BorderThickness="0" Background="Transparent" Padding="5,2" VerticalAlignment="Center"/>
            </Border>
        </StackPanel>

        <UniformGrid Grid.Row="2" Margin="0,30,0,0">

            <Button Click="Click_BtnSend" Content="{x:Static language:Lang.GlobalSend}"  HorizontalAlignment="Right" VerticalAlignment="Center" Height="65" Width="135" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="16" Foreground="White" BorderBrush="{x:Null}" Margin="0,0,33,0">
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/Images/rockConfirm.png"/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
             BorderBrush="{TemplateBinding BorderBrush}"
             BorderThickness="{TemplateBinding BorderThickness}"
             CornerRadius="13">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Click="Click_BtnCancel" Content="{x:Static language:Lang.GlobalCancelText}"  HorizontalAlignment="Right" VerticalAlignment="Center" Height="65" Width="135" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="16" Foreground="White" BorderBrush="{x:Null}" Margin="0,0,33,0">
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/Images/rockCancel.png"/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
             BorderBrush="{TemplateBinding BorderBrush}"
             BorderThickness="{TemplateBinding BorderThickness}"
             CornerRadius="13">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </UniformGrid>

    </Grid>

</Window>
