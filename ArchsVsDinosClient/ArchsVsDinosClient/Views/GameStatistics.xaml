<Window x:Class="ArchsVsDinosClient.Views.GameStatistics"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArchsVsDinosClient.Views"
        xmlns:language="clr-namespace:ArchsVsDinosClient.Properties.Langs"
        mc:Ignorable="d"
        Title="GameStatistics" 
        Height="680" Width="900"
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFF0F0F0" Offset="0"/>
            <GradientStop Color="#FFFCEC5A" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" 
               Content="{x:Static language:Lang.Statistics_Title}" 
               FontFamily="{StaticResource Ouroboros}" 
               FontSize="32" FontWeight="Bold" 
               Foreground="#FF671414" 
               Margin="0,10,0,15"/>

        <Border Grid.Row="1" Background="#FFE64A19" CornerRadius="5" Padding="8" Margin="0,0,0,15"
                Visibility="{Binding HasWarning, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="⚠️" Foreground="White" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding WarningMessage}" Foreground="White" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ItemsControl Grid.Row="2" 
                      ItemsSource="{Binding PlayerStats}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4" Rows="1"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="10" VerticalAlignment="Bottom">

                        <Label Content="{Binding Position, StringFormat='{}{0}°'}"
                               FontFamily="Tw Cen MT Condensed Extra Bold"
                               FontSize="{Binding Position, Converter={StaticResource PositionToFontSizeConverter}}"
                               HorizontalAlignment="Center"
                               FontWeight="Bold"
                               Foreground="{Binding Position, Converter={StaticResource PositionToColorConverter}}"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,10">
                            <Label Content="{Binding Username}" 
                                   FontFamily="Tw Cen MT Condensed Extra Bold" 
                                   FontSize="16" 
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>

                            <TextBlock Text="{Binding Icon}" 
                                       FontSize="20" 
                                       Margin="5,0,0,0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>

                        <Border Width="120"
                                Height="{Binding Position, Converter={StaticResource PositionToHeightConverter}}"
                                BorderBrush="{Binding Position, Converter={StaticResource PositionToBorderConverter}}"
                                BorderThickness="{Binding Position, Converter={StaticResource PositionToBorderThicknessConverter}}"
                                CornerRadius="5" 
                                Margin="0,0,0,10">

                            <Border.Background>
                                <ImageBrush ImageSource="{Binding ProfileImage}" Stretch="UniformToFill"/>
                            </Border.Background>
                        </Border>

                        <Border
                                BorderBrush="{Binding Position, Converter={StaticResource PositionToBorderConverter}}"
                                BorderThickness="{Binding Position, Converter={StaticResource PositionToStatsBorderThicknessConverter}}"
                                Padding="10" 
                                CornerRadius="5">

                            <StackPanel>
                                <Label Content="{x:Static language:Lang.Statistics_EliminatedArchs}"
                                       FontFamily="Trebuchet MS" 
                                       FontSize="12" 
                                       HorizontalAlignment="Center"/>

                                <Label Content="{Binding Points}" 
                                       FontFamily="Tw Cen MT Condensed Extra Bold" 
                                       FontSize="16" 
                                       HorizontalAlignment="Center" 
                                       FontWeight="Bold"/>

                            </StackPanel>
                        </Border>

                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="3">

            <Label Content="{Binding TimerText}" FontSize="16" FontFamily="Trebuchet MS"
               Foreground="#FF671414" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="0,5,0,0" FontStyle="Italic"/>

            <Button Click="Click_BtnExit"  Content="{x:Static language:Lang.GlobalExitText}" Height="60" Width="200" 
                    Foreground="White" FontWeight="Bold" FontFamily="Tw Cen MT Condensed Extra Bold" 
                    FontSize="20" HorizontalAlignment="Right" Margin="0,5,0,0"  BorderBrush="{x:Null}">

                <Button.Background>
                    <ImageBrush ImageSource="/Resources/Images/rockIron.png"/>
                </Button.Background>

                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </Grid>

    </Grid>
</Window>