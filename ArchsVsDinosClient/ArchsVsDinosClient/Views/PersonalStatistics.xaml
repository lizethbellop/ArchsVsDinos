<Window x:Class="ArchsVsDinosClient.Views.PersonalStatistics"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArchsVsDinosClient.Views"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:ArchsVsDinosClient.Properties.Langs"
        Title="PersonalStatistics" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFF0F0F0" Offset="0"/>
            <GradientStop Color="#FFE0E0E0" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <Label Grid.Row="0" Content="{x:Static language:Lang.PlayerStatistics_Title}" 
               FontFamily="{StaticResource Volcanic Dungeon}"
               FontSize="20" FontWeight="Bold" Foreground="Black" Margin="0,10,0,20"/>

        <!-- Estadísticas Generales del Jugador -->
        <Border Grid.Row="1" Background="White" BorderBrush="Gray" BorderThickness="1" 
                CornerRadius="5" Padding="15" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="{Binding TotalMatchesText}" FontFamily="Trebuchet MS" 
                           FontSize="14" Margin="0,5"/>
                <TextBlock Text="{Binding WinRateText}" FontFamily="Trebuchet MS" 
                           FontSize="14" Margin="0,5"/>
                <TextBlock Text="{Binding TotalPointsText}" FontFamily="Trebuchet MS" 
                           FontSize="14" Margin="0,5"/>
                <TextBlock Text="{Binding WinsLossesText}" FontFamily="Trebuchet MS" 
                           FontSize="14" Margin="0,5"/>
            </StackPanel>
        </Border>

        <!-- Contenido Principal -->
        <Border Grid.Row="2" Background="White" BorderBrush="Gray" BorderThickness="1" 
                CornerRadius="5" Padding="10">
            <Grid>
                <!-- Indicador de carga -->
                <TextBlock Text="Cargando estadísticas..." 
                           FontFamily="Trebuchet MS" FontSize="18" 
                           Foreground="Gray" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Mensaje de error -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           FontFamily="Trebuchet MS" FontSize="16" 
                           Foreground="Red" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="20">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasError}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- TabControl con Leaderboard e Historial -->
                <TabControl SelectedIndex="{Binding SelectedTabIndex}">
                    <TabControl.Style>
                        <Style TargetType="TabControl">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TabControl.Style>

                    <!-- Tab Leaderboard -->
                    <TabItem Header="{x:Static language:Lang.Statistics_Leaderboard}" FontFamily="Trebuchet MS" FontSize="14">
                        <ListBox ItemsSource="{Binding Leaderboard}" 
                                 BorderThickness="0" 
                                 Background="Transparent">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="100"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" 
                                                       Text="{Binding Position, StringFormat='#{0}'}" 
                                                       FontWeight="Bold" FontSize="14"
                                                       VerticalAlignment="Center"/>

                                            <TextBlock Grid.Column="1" 
                                                       Text="{Binding Username}" 
                                                       FontSize="14"
                                                       VerticalAlignment="Center"/>

                                            <TextBlock Grid.Column="2" 
                                                       Text="{Binding TotalWins, StringFormat='Victorias: {0}'}" 
                                                       FontSize="12"
                                                       VerticalAlignment="Center"/>

                                            <TextBlock Grid.Column="3" 
                                                       Text="{Binding TotalPoints, StringFormat='Puntos: {0}'}" 
                                                       FontSize="12"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>

                    <!-- Tab Historial -->
                    <TabItem Header="{x:Static language:Lang.Statistics_History}" FontFamily="Trebuchet MS" FontSize="14">
                        <ListBox ItemsSource="{Binding MatchHistory}" 
                                 BorderThickness="0" 
                                 Background="Transparent">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="LightGray" BorderThickness="1" 
                                            CornerRadius="5" Padding="10" Margin="5">
                                        <StackPanel>
                                            <TextBlock Text="{Binding MatchDate, StringFormat='Fecha: {0:dd/MM/yyyy HH:mm}'}" 
                                                       FontWeight="Bold" FontSize="13"/>

                                            <!-- Victoria (Verde) -->
                                            <TextBlock FontSize="12" Margin="0,3,0,0" Foreground="Green">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Won}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                                <Run Text="Resultado: "/>
                                                <Run Text="Victoria" FontWeight="Bold"/>
                                            </TextBlock>

                                            <!-- Derrota (Rojo) -->
                                            <TextBlock FontSize="12" Margin="0,3,0,0" Foreground="Red">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Won}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                                <Run Text="Resultado: "/>
                                                <Run Text="Derrota" FontWeight="Bold"/>
                                            </TextBlock>

                                            <TextBlock Text="{Binding Position, StringFormat='Posición: {0}°'}" 
                                                       FontSize="12" Margin="0,3,0,0"/>
                                            <TextBlock Text="{Binding Points, StringFormat='Puntos: {0}'}" 
                                                       FontSize="12" Margin="0,3,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </TabItem>
                </TabControl>
            </Grid>
        </Border>

        <!-- Botones -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Actualizar" Command="{Binding RefreshCommand}"
                    Height="50" Width="110" Foreground="White" FontWeight="Bold" 
                    BorderBrush="Transparent" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="16"
                    Margin="0,0,10,0">
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/Images/rockConfirm.png"/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="5" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Content="{x:Static language:Lang.GlobalCloseText}" Click="Btn_Close" 
                    Height="50" Width="110" Foreground="White" FontWeight="Bold" 
                    BorderBrush="Transparent" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="16">
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/Images/rockCancel.png"/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="5" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
