<Page x:Class="ArchsVsDinosClient.Views.Instructions.Page19"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ArchsVsDinosClient.Views.Instructions"
      xmlns:language="clr-namespace:ArchsVsDinosClient.Properties.Langs"
      mc:Ignorable="d" 
      d:DesignHeight="580" d:DesignWidth="1100"
      Title="Page19">

    <Page.Background>
        <ImageBrush ImageSource="/Resources/Images/Instructions/19.png" Stretch="Fill"/>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.4*"/>
            <RowDefinition Height="11*"/>
            <RowDefinition Height="1.2*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <TextBlock Text="{x:Static language:Lang.Instructions19_1}"
           FontFamily="Tw Cen MT Condensed Extra Bold"
           TextAlignment="Center" FontSize="40"
           HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" Width="448" TextWrapping="Wrap"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Click="Click_BtnBack" Width="65" Height="45" Margin="0,0,0,5">
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/Images/flechaA.png"/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"  CornerRadius="5" BorderThickness="0">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button Grid.Column="2" Click="Click_BtnNext" Width="65" Height="45" Margin="0,0,0,5">
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/Images/flechaS.png"/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"  CornerRadius="5" BorderThickness="0">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </Grid>
    </Grid>
</Page>
