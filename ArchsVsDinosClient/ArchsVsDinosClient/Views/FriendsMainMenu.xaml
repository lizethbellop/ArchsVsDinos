<Window x:Class="ArchsVsDinosClient.Views.FriendsMainMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArchsVsDinosClient.Views"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:ArchsVsDinosClient.Properties.Langs"
        Title="FriendsMainMenu" Height="500" Width="550">

    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FF159ED6"/>
            <GradientStop Color="#FFE8FFBB" Offset="0.988"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,10,0,15" HorizontalAlignment="Center">
            <Label Content="{x:Static language:Lang.FriendsMainMenu_FriendsTitle}" FontFamily="{StaticResource Ouroboros}" FontSize="32" FontWeight="Bold" Foreground="#FF2C5122"/>
        </StackPanel>

        <Border Grid.Row="1" CornerRadius="10" BorderBrush="#FF196572" BorderThickness="2" Margin="0,0,0,10" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#FF6ABA2A" CornerRadius="8,8,0,0" Padding="10,5">
                    <Label Content="{x:Static language:Lang.Friends_MyFriends}" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="18" Foreground="White" HorizontalAlignment="Center"/>
                </Border>

                <ListBox Grid.Row="1" x:Name="LbFriends" BorderThickness="0" Background="Transparent" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="5">
                    <ListBox.Foreground>
                        <ImageBrush/>
                    </ListBox.Foreground>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5" Padding="10" CornerRadius="8" Background="White" BorderBrush="#FFE0E0E0" BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#FFEAFFBB" Offset="0"/>
                                                            <GradientStop Color="#FFD4F5FF" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="35" Height="35" CornerRadius="17.5" Background="#FF6ABA2A" Margin="0,0,10,0">
                                        <Label Content="👤" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0"/>
                                    </Border>

                                    <TextBlock Grid.Column="1" Text="{Binding}" FontFamily="Trebuchet MS" FontSize="16" VerticalAlignment="Center" Foreground="#FF196572" FontWeight="Bold"/>

                                    <Button Grid.Column="2" Content="{x:Static language:Lang.FriendsMainMenu_EliminateFriend}" Width="80" Height="30" FontSize="14" FontWeight="Bold"
                                            Background="#FFD32F2F" Foreground="White" BorderBrush="Transparent" FontFamily="Tw Cen MT Condensed Extra Bold">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                                        CornerRadius="5" BorderThickness="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>

                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#FFB71C1C"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#FF8B0000"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2"  Margin="0,0,0,-30" HorizontalAlignment="Center">
            <UniformGrid>
                <Label Content="{x:Static language:Lang.Friends_AddFriend}" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="18" Foreground="#FF196572" HorizontalAlignment="Center"/>
            </UniformGrid>
            <UniformGrid Margin="20,0,20,0" HorizontalAlignment="Center">
                <Border CornerRadius="15" Background="White" BorderBrush="Gray" BorderThickness="1" Width="200" Height="29">
                    <TextBox x:Name="TbEmail" TextWrapping="NoWrap" FontFamily="Trebuchet MS" BorderThickness="0" Background="Transparent" Padding="5,2" Text="example@email.com"/>
                </Border>
                <Button Content="{x:Static language:Lang.Friends_Add}" Height="45" Width="80" Foreground="White" FontWeight="Bold"
                     BorderBrush="Transparent" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="16">
                    <Button.Background>
                        <ImageBrush ImageSource="/Resources/Images/rockIron.png"/>
                    </Button.Background>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="13">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </UniformGrid>

        </StackPanel>

        <UniformGrid Grid.Row="3" HorizontalAlignment="Center">

            <Button Content="{x:Static language:Lang.GlobalCloseText}" Click="Btn_Close" Margin="5" Height="60" Width="120" Foreground="White" FontWeight="Bold" 
                    BorderBrush="Transparent" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="16">
                <Button.Background>
                    <ImageBrush ImageSource="/Resources/Images/rockCancel.png"/>
                </Button.Background>
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="13">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </UniformGrid>
    </Grid>
</Window>
